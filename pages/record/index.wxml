<!-- logs.wxml -->
<!-- <view class="container log-list">
  <block wx:for="{{logs}}" wx:for-item="log">
    <text class="log-item">{{index + 1}}. {{log}}</text>
  </block>
</view> -->
<view class="record-container">
  <nav class="nav">
    <view class="weui-tab">
      <view class="weui-navbar">
        <block wx:for="{{tabs}}" wx:key="*this">
          <view id="{{index}}" class="weui-navbar__item {{activeIndex == index ? 'weui-bar__item_on' : ''}}" bindtap="tabClick">
            <view class="weui-navbar__title">{{item}}</view>
          </view>
        </block>
      </view>
      <view class="weui-tab__panel">
        <view class="weui-tab__content" hidden="{{false}}">
          <view class="line-container">
            <ec-canvas id="mychart-dom-line" canvas-id="mychart-line" ec="{{ ecBar }}"></ec-canvas>
          </view>
          <!-- <block>睡眠时间统计</block>
          <view class="bar-container">
            <ec-canvas id="mychart-dom-bar" canvas-id="mychart-bar" ec="{{ ecBar }}"></ec-canvas>
          </view> -->
        </view>
        <!-- <block>平均早睡</block>
        <block>平均早起</block>
        <block>平均睡眠</block> -->
        <!-- <view class="weui-tab__content" hidden="{{activeIndex != 1}}">选项二的内容</view>
        <view class="weui-tab__content" hidden="{{activeIndex != 2}}">选项三的内容</view> -->
      </view>
    </view>
  </nav>
  <!-- <view>打卡统计日历</view>
  <view class="calendar-container">
    <ec-canvas id="mychart-dom-calendar" canvas-id="mychart-calendar" ec="{{ ecCalendar }}"></ec-canvas>
  </view> -->
  <!-- <view>
    <view>打卡统计</view>
    <view>最早起床，最晚起床，连续早起，累计早起</view>
    <view>最早睡觉，最晚睡觉，连续早睡，累计早睡</view>
  </view> -->
</view>